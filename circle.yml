machine:
  python:
    version: 2.7.5
  node:
    version: 8.9.1 

dependencies:
  override:
    - ./setup.sh

test:
  override:
    - (mkdir ~/.codalab && printf '{}' > $HOME/.codalab/website-config.json)
