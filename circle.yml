machine:
  python:
    version: 2.7.5

dependencies:
  override:
    - mkdir ~/.codalab
    - ./setup.sh

test:
  override:
    - (printf '{}' > $HOME/.codalab/website-config.json)
    - (cd codalab && ./manage syncdb --migrate && ./manage get_users)
